---
name: Observability

on:
  pull_request:
    paths:
      - "platform-services/observability/**"
      - ".github/workflows/observability.yml"

permissions:
  contents: read

jobs:
  observability:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      # Enable when Prometheus/Grafana/Loki are added
      #
      # - name: Helm Lint
      #
      # - name: Kustomize Build