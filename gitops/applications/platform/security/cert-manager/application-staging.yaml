apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: cert-manager-staging
  namespace: argocd

  labels:
    app.kubernetes.io/name: cert-manager
    app.kubernetes.io/component: security
    app.kubernetes.io/part-of: tuskerblueprint
    app.kubernetes.io/environment: staging

  annotations:
    argocd.argoproj.io/sync-wave: "10"

spec:
  project: platform

  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager-staging

  sources:
    - repoURL: https://charts.jetstack.io
      chart: cert-manager
      targetRevision: v1.21.0
      helm:
        releaseName: cert-manager-staging
        valueFiles:
          - $values/platform-services/cert-manager/values/staging.yaml
    - repoURL: git@github.com:stonetusker/tuskerblueprint.git
      targetRevision: main
      ref: values

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - PrunePropagationPolicy=foreground
