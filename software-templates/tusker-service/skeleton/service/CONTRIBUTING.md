# Contributing

1. Create a feature branch from `main`.
2. Run `scripts/verify.sh` locally.
3. Push the branch and open a pull request.
4. Wait for every required CI and security check.
5. Ask an owner to review and merge the change.

Do not commit credentials, kubeconfig files, private keys, generated coverage files, or local environment files.
