apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: platform-secret-reader
  namespace: platform-secrets
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["ghcr-pull-credentials"]
    verbs: ["get"]
