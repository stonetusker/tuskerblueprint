# ==============================================================================
# TuskerBlueprint
# Grafana Helm Values
# Environment: Development
# ==============================================================================

adminUser: admin
# 1. Define a static password so Helm doesn't inject a random one on restart
adminPassword: "admin123"

# 2. Enable persistence so the SQLite database (users, custom dashboards) survives reboots
persistence:
  enabled: true
  size: 5Gi
  # storageClassName: local-path # Uncomment if your K3s uses a specific storage class

service:
  type: ClusterIP

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

datasources:
  datasources.yaml:
    apiVersion: 1
    deleteDatasources:
      - name: Prometheus
        orgId: 1
      - name: Loki
        orgId: 1
      - name: Tempo
        orgId: 1
    datasources:
      - name: Prometheus
        uid: prometheus
        type: prometheus
        access: proxy
        url: http://prometheus-server.monitoring.svc.cluster.local
        isDefault: true
        editable: false
        jsonData:
          # Must match Prometheus server.global.scrape_interval. Grafana uses
          # this value to calculate a safe $__rate_interval for counters.
          timeInterval: 15s
          httpMethod: POST
      - name: Loki
        uid: loki
        type: loki
        access: proxy
        url: http://loki-gateway.monitoring.svc.cluster.local
        editable: false
        jsonData:
          derivedFields:
            - datasourceUid: tempo
              matcherRegex: 'correlation_id["\s:]+[=: ]*"?(?P<correlation_id>[A-Za-z0-9-]+)'
              name: Trace by correlation ID
              url: '$__value.raw'
              urlDisplayLabel: 'View trace'
      - name: Tempo
        uid: tempo
        type: tempo
        access: proxy
        url: http://tempo.monitoring.svc.cluster.local:3200
        editable: false
        jsonData:
          tracesToLogsV2:
            datasourceUid: loki
            spanStartTimeShift: "-5m"
            spanEndTimeShift: "5m"
            filterByTraceID: true
            filterBySpanID: false

dashboardProviders:
  dashboardproviders.yaml:
    apiVersion: 1
    providers:
      - name: default
        orgId: 1
        folder: Stonetusker Demo
        type: file
        disableDeletion: true
        editable: false
        updateIntervalSeconds: 30
        options:
          path: /var/lib/grafana/dashboards/default

# The ConfigMap is reconciled by the third source in the Grafana Argo CD
# Application. Keeping JSON outside Helm values makes dashboard validation and
# future reviews deterministic.
dashboardsConfigMaps:
  default: grafana-dashboard-demo-service