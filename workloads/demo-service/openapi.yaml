openapi: 3.0.3
info:
  title: StoneTusker Customer Notification API
  version: 1.0.0
  description: >-
    Deterministic reference API used to demonstrate secure delivery, GitOps,
    observability, failure detection, rollback, and Backstage API discovery.
    The service does not send real notifications.
servers:
  - url: http://demo-service.demo-service-development.svc.cluster.local
    description: Development cluster service
paths:
  /:
    get:
      summary: Return service and release metadata
      operationId: getServiceMetadata
      responses:
        '200':
          description: Service metadata
          headers:
            X-Correlation-ID:
              schema:
                type: string
            X-Service-Version:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceMetadata'
  /healthz:
    get:
      summary: Liveness endpoint
      operationId: getHealth
      responses:
        '200':
          description: Process is alive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Status'
  /readyz:
    get:
      summary: Readiness endpoint
      operationId: getReadiness
      responses:
        '200':
          description: Service can receive traffic
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Status'
        '503':
          description: Controlled readiness failure or unavailable dependency
  /api/v1/notifications:
    post:
      summary: Accept a fictional notification request
      operationId: createNotification
      parameters:
        - in: header
          name: X-Demo-Request
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationRequest'
      responses:
        '202':
          description: Notification accepted for the demonstration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationRecord'
        '422':
          description: Invalid request
        '500':
          description: Controlled demo error mode
  /api/v1/notifications/{notificationId}:
    get:
      summary: Return one in-memory notification record
      operationId: getNotification
      parameters:
        - in: path
          name: notificationId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Notification record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationRecord'
        '404':
          description: Notification not found
components:
  schemas:
    Status:
      type: object
      required: [status]
      properties:
        status:
          type: string
    ServiceMetadata:
      type: object
      required: [service, environment, version, status]
      properties:
        service:
          type: string
        environment:
          type: string
        version:
          type: string
        status:
          type: string
    NotificationRequest:
      type: object
      required: [channel, recipient, message]
      properties:
        channel:
          type: string
          enum: [email, sms, webhook]
        recipient:
          type: string
          minLength: 3
          maxLength: 200
        message:
          type: string
          minLength: 1
          maxLength: 2000
    NotificationRecord:
      allOf:
        - $ref: '#/components/schemas/NotificationRequest'
        - type: object
          required: [id, state, accepted_at, correlation_id]
          properties:
            id:
              type: string
              format: uuid
            state:
              type: string
              example: accepted
            accepted_at:
              type: string
              format: date-time
            correlation_id:
              type: string
              format: uuid
